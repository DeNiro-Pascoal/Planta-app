import { StyleSheet, Button } from "react-native";

trocardemodo = () => {
  //trocar de estilos normal e noturno
};

<Button onPress={trocardemodo} title="mudar de modo" />;

const estilobotao = StyleSheet.create({
  //add estilos do botao
});
